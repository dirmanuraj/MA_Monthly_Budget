{
  "name": "budget-dashboard",
  "version": "1.0.0",
  "description": "Monthly expense dashboard with GitHub-backed persistence, deployable on Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
